# ai-stack — แม่แบบ env. คัดลอกเป็น .env (gitignored) แล้วเติมค่า
# ใช้ใน Phase D (OpenClaw hybrid brain + แชต)

# Cloud API สำหรับงาน agent หนัก (งานลับ → ใช้ Ollama local แทน)
# ANTHROPIC_API_KEY=

# ช่องแชต (Phase D) — ตั้ง allowlist ผู้ใช้ที่สั่งงานได้เสมอ (decision #8)
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_ALLOWED_USER_IDS=
# LINE_CHANNEL_ACCESS_TOKEN=
# LINE_CHANNEL_SECRET=

# Ollama (ปกติไม่ต้องแก้)
OLLAMA_HOST=127.0.0.1:11434
